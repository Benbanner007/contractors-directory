---
// No props needed for now
---

<header class="site-header">
  <nav class="navbar">
    <div class="container">
      <div class="nav-content">
        <!-- Logo -->
        <div class="nav-brand">
          <a href="/" class="brand-link">
            <svg class="brand-logo" width="40" height="40" viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">
              <!-- Modern Triangle Building -->
              <defs>
                <linearGradient id="buildingGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" style="stop-color:var(--primary-500);stop-opacity:1" />
                  <stop offset="100%" style="stop-color:var(--primary-700);stop-opacity:1" />
                </linearGradient>
                <linearGradient id="toolGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" style="stop-color:var(--accent-400);stop-opacity:1" />
                  <stop offset="100%" style="stop-color:var(--accent-600);stop-opacity:1" />
                </linearGradient>
              </defs>
              
              <!-- Main building structure -->
              <path d="M24 6L38 32H10L24 6Z" fill="url(#buildingGradient)" stroke="var(--primary-800)" stroke-width="1.5"/>
              
              <!-- Building details -->
              <rect x="22" y="14" width="4" height="6" fill="var(--primary-100)" rx="0.5"/>
              <rect x="18" y="18" width="3" height="4" fill="var(--primary-100)" rx="0.5"/>
              <rect x="27" y="18" width="3" height="4" fill="var(--primary-100)" rx="0.5"/>
              <rect x="20" y="24" width="8" height="6" fill="var(--primary-800)" rx="1"/>
              
              <!-- Professional tools badge -->
              <circle cx="35" cy="13" r="7" fill="url(#toolGradient)" stroke="white" stroke-width="2"/>
              
              <!-- Hammer and wrench crossed -->
              <g transform="translate(35,13)">
                <!-- Hammer -->
                <path d="M-3 -2L-1 -4L3 0L1 2L-3 -2Z" fill="white"/>
                <rect x="-4" y="-1" width="2" height="6" fill="white" rx="0.5"/>
                <!-- Wrench -->
                <path d="M2 -3L4 -1L0 3L-2 1L2 -3Z" fill="white" opacity="0.9"/>
                <circle cx="1.5" cy="-1.5" r="1" fill="none" stroke="white" stroke-width="0.8"/>
              </g>
              
              <!-- Subtle glow effect -->
              <circle cx="24" cy="20" r="16" fill="var(--primary-500)" opacity="0.1"/>
            </svg>
            <span class="brand-text">Triangle Contractors Hub</span>
          </a>
        </div>

        <!-- Mobile Menu Button -->
        <button class="mobile-menu-btn" onclick="toggleMobileMenu()">
          <span class="hamburger"></span>
          <span class="hamburger"></span>
          <span class="hamburger"></span>
        </button>

        <!-- Navigation Links -->
        <div class="nav-links" id="nav-links">
          <a href="/" class="nav-link">Home</a>
          <a href="/contractors" class="nav-link">Find Contractors</a>
          
          <!-- Categories Dropdown -->
          <div class="nav-dropdown">
            <a href="/categories" class="nav-link dropdown-toggle">
              Categories
              <span class="dropdown-arrow">▼</span>
            </a>
            <div class="dropdown-menu">
              <a href="/categories/plumbing" class="dropdown-item">
                <span class="dropdown-icon">🔧</span>
                Plumbing
              </a>
              <a href="/categories/electrical" class="dropdown-item">
                <span class="dropdown-icon">⚡</span>
                Electrical
              </a>
              <a href="/categories/roofing" class="dropdown-item">
                <span class="dropdown-icon">🏠</span>
                Roofing
              </a>
              <a href="/categories/hvac" class="dropdown-item">
                <span class="dropdown-icon">❄️</span>
                HVAC
              </a>
              <a href="/categories/general-contractor" class="dropdown-item">
                <span class="dropdown-icon">🏗️</span>
                General Contractors
              </a>
              <a href="/categories/landscaping" class="dropdown-item">
                <span class="dropdown-icon">🌿</span>
                Landscaping
              </a>
              <a href="/categories/painting" class="dropdown-item">
                <span class="dropdown-icon">🎨</span>
                Painting
              </a>
              <a href="/categories/flooring" class="dropdown-item">
                <span class="dropdown-icon">🏢</span>
                Flooring
              </a>
            </div>
          </div>

          <a href="/blog" class="nav-link">Tips & Advice</a>
          <a href="/contractors" class="nav-link nav-cta">Get Quotes</a>
        </div>
      </div>
    </div>
  </nav>
</header>

<style>
  .site-header {
    background: white;
    box-shadow: var(--shadow-sm);
    position: sticky;
    top: 0;
    z-index: 1000;
    border-bottom: 1px solid var(--gray-200);
  }

  .navbar {
    padding: var(--space-4) 0;
  }

  .nav-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  /* Brand */
  .nav-brand {
    flex-shrink: 0;
  }

  .brand-link {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    text-decoration: none;
    color: var(--gray-900);
    font-weight: 700;
    font-size: var(--font-size-lg);
  }

  .brand-link:hover {
    text-decoration: none;
    color: var(--primary-600);
  }

  .brand-logo {
    width: 40px;
    height: 40px;
    flex-shrink: 0;
    transition: var(--transition);
  }

  .brand-link:hover .brand-logo {
    transform: scale(1.05);
  }

  .brand-text {
    white-space: nowrap;
  }

  /* Mobile Menu Button */
  .mobile-menu-btn {
    display: none;
    flex-direction: column;
    background: none;
    border: none;
    cursor: pointer;
    padding: var(--space-2);
    gap: 4px;
  }

  .hamburger {
    width: 24px;
    height: 3px;
    background: var(--primary-900);
    transition: var(--transition);
    border-radius: 2px;
  }

  .mobile-menu-btn.active .hamburger:nth-child(1) {
    transform: rotate(45deg) translate(6px, 6px);
  }

  .mobile-menu-btn.active .hamburger:nth-child(2) {
    opacity: 0;
  }

  .mobile-menu-btn.active .hamburger:nth-child(3) {
    transform: rotate(-45deg) translate(6px, -6px);
  }

  /* Navigation Links */
  .nav-links {
    display: flex;
    align-items: center;
    gap: var(--space-6);
  }

  .nav-link {
    color: var(--gray-700);
    text-decoration: none;
    font-weight: 500;
    padding: var(--space-2) var(--space-3);
    border-radius: var(--border-radius);
    transition: var(--transition);
    white-space: nowrap;
  }

  .nav-link:hover {
    color: var(--primary-600);
    text-decoration: none;
    background: var(--gray-100);
  }

  .nav-cta {
    background: var(--accent-500);
    color: white;
    font-weight: 600;
    border: 2px solid var(--accent-500);
  }

  .nav-cta:hover {
    background: var(--accent-600);
    color: white;
    border-color: var(--accent-600);
  }

  /* Dropdown */
  .nav-dropdown {
    position: relative;
  }

  .dropdown-toggle {
    display: flex;
    align-items: center;
    gap: var(--space-1);
  }

  .dropdown-arrow {
    font-size: var(--font-size-xs);
    transition: var(--transition);
  }

  .nav-dropdown:hover .dropdown-arrow {
    transform: rotate(180deg);
  }

  .dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    background: white;
    min-width: 220px;
    box-shadow: var(--shadow-lg);
    border-radius: var(--border-radius);
    padding: var(--space-3);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.2s ease;
    z-index: 1001;
    border: 1px solid var(--gray-200);
  }

  .nav-dropdown:hover .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .dropdown-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3);
    color: var(--gray-700);
    text-decoration: none;
    border-radius: var(--border-radius);
    transition: var(--transition);
  }

  .dropdown-item:hover {
    background: var(--gray-100);
    color: var(--primary-600);
    text-decoration: none;
  }

  .dropdown-icon {
    font-size: var(--font-size-base);
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .nav-links {
      gap: var(--space-4);
    }

    .brand-text {
      display: none;
    }
  }

  @media (max-width: 768px) {
    .mobile-menu-btn {
      display: flex;
    }

    .nav-links {
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      background: white;
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      padding: var(--space-4);
      box-shadow: var(--shadow-lg);
      opacity: 0;
      visibility: hidden;
      transform: translateY(-10px);
      transition: all 0.3s ease;
      border: 1px solid var(--gray-200);
      border-top: none;
    }

    .nav-links.active {
      opacity: 1;
      visibility: visible;
      transform: translateY(0);
    }

    .nav-link {
      padding: var(--space-3);
      border-bottom: 1px solid var(--gray-100);
    }

    .nav-link:last-child {
      border-bottom: none;
    }

    .nav-dropdown .dropdown-menu {
      position: static;
      opacity: 1;
      visibility: visible;
      transform: none;
      box-shadow: none;
      background: var(--gray-50);
      margin-top: var(--space-2);
      border: 1px solid var(--gray-200);
    }

    .dropdown-toggle .dropdown-arrow {
      display: none;
    }

    .brand-text {
      display: inline;
      font-size: var(--font-size-base);
    }
  }
</style>

<script>
  function toggleMobileMenu() {
    const navLinks = document.getElementById('nav-links');
    const menuBtn = document.querySelector('.mobile-menu-btn');
    
    if (navLinks && menuBtn) {
      navLinks.classList.toggle('active');
      menuBtn.classList.toggle('active');
    }
  }

  // Close mobile menu when clicking outside
  document.addEventListener('click', function(event) {
    const navLinks = document.getElementById('nav-links');
    const menuBtn = document.querySelector('.mobile-menu-btn');
    const navbar = document.querySelector('.navbar');
    
    if (navbar && navLinks && menuBtn && !navbar.contains(event.target as Node)) {
      navLinks.classList.remove('active');
      menuBtn.classList.remove('active');
    }
  });

  // Close mobile menu when window is resized to desktop
  window.addEventListener('resize', function() {
    if (window.innerWidth > 768) {
      const navLinks = document.getElementById('nav-links');
      const menuBtn = document.querySelector('.mobile-menu-btn');
      
      if (navLinks && menuBtn) {
        navLinks.classList.remove('active');
        menuBtn.classList.remove('active');
      }
    }
  });
</script>